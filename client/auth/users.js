function validateUser(user_id, auth_token) {
  // TODO: validate user's session
}

function signUserIn(user_id, password) {
  // TODO: sign into user's account.
}

function signUserOut() {
  // TODO: sign the user out.
}
